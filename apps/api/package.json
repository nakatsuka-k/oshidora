{
  "name": "oshidora-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "stream:create-signing-key": "node ./scripts/create-stream-signing-key.mjs",
    "db:migrate:local": "wrangler d1 migrations apply oshidora-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply oshidora-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.54.0"
  }
}
