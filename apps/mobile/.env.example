# Web
# EXPO_PUBLIC_API_BASE_URL=http://localhost:8787

# iOS Simulator
# EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8787

# Android Emulator
# EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:8787

# Physical device (replace with your computer's LAN IP)
# EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8787

# Optional: public web entrypoint for share URLs when running on native.
# If omitted, native share falls back to API /share/* pages.
# EXPO_PUBLIC_WEB_BASE_URL=https://your-web-app.example/

# Login (temporary client-side check)
# EXPO_PUBLIC_LOGIN_EMAIL=test@example.com
# EXPO_PUBLIC_LOGIN_PASSWORD=password

# Cloudflare Stream (private playback / Signed URL) sample in 作品詳細(workDetail)
# NOTE: Stream API token & signing key must NOT be put in EXPO_PUBLIC_* (client-side).
EXPO_PUBLIC_CLOUDFLARE_STREAM_SAMPLE_VIDEO_ID=75f3ddaf69ff44c43746c9492c3c4df5

# Assets uploader (R2 direct upload via Workers)
# If EXPO_PUBLIC_UPLOADER_JWT is set, the app uploads to this endpoint.
# If JWT is missing or invalid, the app falls back to API upload.
EXPO_PUBLIC_UPLOADER_BASE_URL=https://assets-uploader.oshidra.com/
# EXPO_PUBLIC_UPLOADER_JWT=...

# Web (Cloudflare Pages IP allowlist)
# Comma-separated IPs; used during `npm run build:web` to generate dist/_worker.js
# OSHIDORA_ALLOWED_IPS=159.28.175.137,223.135.200.51,117.102.205.215,133.232.96.225,3.114.72.126
