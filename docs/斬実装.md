未実装/ダミーが残っている箇所を正確に洗い出すため、Admin側の PlaceholderScreen / RankingPlaceholderScreen / ダミーデータ利用箇所を検索して一覧化します。

残ってる（または要件に対して未達の）画面/機能、現状だとだいたいこのあたりです。

Adminでまだダミー/未実装が残ってる

配信予定動画（CMS_00003/00004相当）：一覧/詳細が固定配列＋ローカルstateのまま（API接続なし）
該当: App.tsx:1569-1705
カテゴリ/タグ（CMS_00031〜00036相当）：一覧/編集が固定配列＋保存がno-op
該当: App.tsx:2990-3145
ランキング（CMS_00011〜00020相当）：RankingPlaceholderScreen のまま（集計テーブル/算出ロジックが別途必要）
該当: App.tsx:4160-4166
ダッシュボード（CMS_00002）のうち、今回 pendingCount は実データ化したけど、KPI（総ユーザー数/公開中作品数など）は — のまま（API側に集計エンドポイントが必要）
画面一覧.mdに書いてあるが、要件レベルでは未着手/未達になりやすいところ

共通UI（CMS_00050〜00057）：404/メンテナンス画面などは「APIはあるが画面切替」は未対応（今は画面内banner表示中心）
お知らせ（CMS_00021〜00023）：一覧/編集は実装済みだが「タグ」「メール（txt/html）」「プッシュ送信」などは現状“保存フラグ”のみで、送信処理は未実装
未承認俳優（CMS_00009/00010）：一覧/詳細＋承認/否認は実装済み。ただし「否認時通知（メール等）」は未実装（要件にある“送信”はこれから）
次に埋める優先度としては、(1) 配信予定動画、(2) カテゴリ/タグ、(3) ダッシュボードKPI集計、(4) 共通（404/メンテナンス表示）あたりが進めやすいです。どれから行きます？